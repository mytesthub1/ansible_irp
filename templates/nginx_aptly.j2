server {
  server_name {{ inventory_hostname }};
  listen 80 default_server;
  root /var/www/aptly/public;

  location / {
    autoindex on;
  }

  location /conf {
    deny all;
  }

  location /db {
    deny all;
  }
}
